# ───────── Prakash Mobile web — environment ─────────
# Copy to .env.local for local dev. On Vercel, set as a Project env var.

# Base URL of the API (NestJS). Local dev defaults to http://localhost:4000.
# In production set to your Railway API domain (no trailing slash).
NEXT_PUBLIC_API_URL="http://localhost:4000"
